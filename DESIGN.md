# Design

## Visual Theme

**โปสเตอร์กีฬา** — สีแบนจัดจ้าน ตัวอักษรใหญ่หนา ไม่มีไล่เฉด ไม่มีเงาฟุ้ง
ทุกอย่างต้องอ่านออกจากหลังห้องเรียน

ธีมสว่างล็อกไว้ (`color-scheme: light`) เพราะจอหลักคือโปรเจกเตอร์ในห้องที่เปิดไฟ
พื้นเป็นสีเทาอมฟ้าอ่อน ไม่ใช่ขาวล้วน เพื่อให้แถบสีของลู่แข่งเด้งออกมา

## Color Palette

| Token | ค่า | ใช้ที่ไหน |
|---|---|---|
| `--ground` | `#EEF1F8` | พื้นหน้าเว็บ |
| `--paper` | `#FFFFFF` | พื้นลู่แข่ง การ์ด แถบล่าง |
| `--ink` | `#0E1426` | แถบบน ตัวอักษร ปุ่มปล่อยตัว |
| `--ink-2` | `#4A5473` | ข้อความรอง |
| `--line` | `#D5DBEA` | ขอบและเส้นคั่น |
| `--blue` | `#0B57F0` | **ทีมเบา** · ปุ่มเดินหน้า · ป้ายชนะ |
| `--amber` | `#E08A00` | **ทีมกลาง** (CH₄) · แถบปี ค.ศ. ของเกรแฮม |
| `--red` | `#DB2216` | **ทีมหนัก** · ตัวคูณของเวลา |

สามสีทีมไล่จากเร็วไปช้า เห็นสีแล้วรู้ทันทีว่าแก๊สไหน
ธงเส้นชัยใช้ลายหมากรุกดำ-ขาว ไม่ใช้สีทีม เพื่อไม่ให้สับสน

## Typography

**Kanit** ตัวเดียวทั้งเว็บ — น้ำหนัก 800 สำหรับหัวเรื่องและตัวเลขคะแนน,
600 สำหรับป้ายกำกับ, 400 สำหรับข้อความ

Kanit เป็นแบบอักษรไทยที่ให้ความรู้สึกป้ายประกาศ/สนามกีฬาได้ตรงที่สุด
ตัวเลขทุกที่ที่ต้องเทียบกันใช้ `font-variant-numeric: tabular-nums`

หัวเรื่องหน้าแรกใหญ่ถึง 76px ตัวเลขเวลาใหญ่ถึง 54px เพื่อให้อ่านจากหลังห้อง

## Layout

โครงเดียวทุกหน้าจอ: แถบบนดำมีเลขสเต็ป 1–5 · เนื้อหากึ่งกลางแนวตั้ง · แถบล่างมีปุ่มเดินหน้า

**ลู่แข่ง** = ตาราง 3 คอลัมน์ `[ป้ายชื่อแก๊ส] [ลู่] [ผลการวัด]`
ความสูงลู่กำหนดเป็นค่าตายตัวด้วย `clamp()` ไม่ใช่ `min-height` เพราะ canvas ข้างในสูง 100%
ถ้าพ่อแม่สูงอัตโนมัติ ความสูงจะอ้างอิงกันไปมาแล้วลู่จะยืดไม่หยุด

หน้าที่ 3 ใช้ลู่เตี้ยกว่า (`.is-trio`) และย้ายปุ่มไปอยู่แถวหัวเรื่อง
เพื่อให้แถบสรุป "มวล ×4 → เวลา ×2" อยู่ในจอเดียวกับลู่แข่ง

หน้าที่ 4 แบ่งสองคอลัมน์บนจอกว้าง: ซ้ายคือที่มาของสูตร ขวาคือเกรแฮมกับเครื่องแทนค่า

## Components

- **ป้ายชื่อแก๊ส** — บล็อกสีทีมที่หัวลู่ มีสูตรเคมีตัวใหญ่และมวลตัวเล็ก เหมือนป้ายชื่อนักกีฬา
- **ลู่** — พื้นขาว ขอบมน ธงหมากรุกวาดในผืนผ้าใบที่ตำแหน่ง `LANE.finish` โมเลกุลที่ผ่านเส้นชัยวิ่งออกนอกลู่ไปเลย ไม่กองรวมกันที่ปลาย
- **ป้ายนักวิ่ง** — แคปซูลสีทีมมีสูตรเคมี วิ่งอยู่บนลู่ที่ตำแหน่งกลุ่มก้อนแก๊ส แล้วหยุดที่เส้นชัย
  **นี่คือองค์ประกอบที่สำคัญที่สุดของหน้าจอ** เพราะเป็นสิ่งเดียวที่ตาต้องไล่ตาม
  และเป็นตัวที่ข้ามเส้นชัยแล้วชนะ ตรงกับค่าที่ระบบใช้วัดเวลาพอดี
- **โมเลกุล** — 800 ตัวต่อลู่ ตัวที่อยู่ในกลุ่มก้อนวาดเข้ม ส่วนตัวที่แหกฝูงนำอยู่ระหว่างทาง
  วาดจางที่ 30% เพื่อไม่ให้ตาไปจับผิดตัวว่าเป็นผู้ชนะ
- **นาฬิกา** — บล็อกแยกของตัวเอง ตัวเลขกว้างตายตัว 3.2 ตัวอักษร ไม่งั้นทุกอย่างในแถวจะขยับทุกครั้งที่ข้ามหลัก
- **ปุ่มสโลว์โมชัน** — กดได้ทั้งก่อนและระหว่างแข่ง มีผลทันที ติดสถานะด้วยสีส้ม
- **ปุ่มปล่อยตัวแบบยืนยันสองครั้ง (ด่าน 3)** — ถ้ามีเวลาที่วัดได้ค้างบนจอ กดครั้งแรกจะเปลี่ยนเป็นสีส้ม
  พร้อมข้อความ "กดอีกครั้งเพื่อวัดใหม่" และคลายเองใน 4 วินาที กันครูกดพลาดทับข้อมูลที่นักเรียนกำลังจด
- **รากที่สอง** — วาดด้วย SVG ที่ยืดเต็มความสูงของสิ่งที่อยู่ใต้ราก ทั้งในสมการใหญ่ ในบรรทัด และในขั้นตอนคำนวณ
  อักขระ √ ธรรมดามีความสูงตายตัว ทำให้ดูเหมือนรากครอบแค่ตัวเศษ ซึ่งผิดความหมายทางคณิตศาสตร์

## Motion

- นับถอยหลัง 3-2-1-ไป! ทับบนลู่ก่อนปล่อยตัว
- ผลการวัดเด้งเข้ามาเมื่อแต่ละลู่วัดผลได้ (`badge-pop`)
- หน้าสูตรเผยทีละบรรทัดห่างกัน 420ms ให้ครูพูดตามได้ทัน
- `prefers-reduced-motion` ตัด transition ทั้งหมด แต่โมเลกุลยังวิ่ง เพราะการวิ่งคือเนื้อหา

## กติกาของเนื้อหาบนจอ

หน้าจอ **ห้ามสรุปแทนนักเรียน** จนกว่าจะถึงด่าน 4 ซึ่งเป็นด่านสรุป กติกานี้เป็นเหตุผลเบื้องหลัง
การตัดสินใจหลายอย่างที่ดูเหมือนขาดข้อมูล แต่จงใจทั้งหมด

- ด่าน 2 บอกแค่ว่าใครถึงเส้นชัยก่อน ไม่บอกว่า "หนักกว่าแพร่ช้ากว่า" เพราะการแข่งคู่เดียวยังสรุปแบบนั้นไม่ได้
- ปุ่มเลือกคู่แข่งมีแค่สูตรเคมี ไม่มีคำว่า "ต่างกันมาก" หรือ "สูสี" กำกับ เพราะนั่นคือคำตอบ
- ด่าน 3 ไม่มีตารางสรุปและไม่มีตัวคูณ มีแต่เวลาข้างลู่ ให้นักเรียนจดลงใบงานแล้วเทียบเอง
- โจทย์ด่าน 5 ไม่มีคำใบ้ว่าให้ใช้วิธีไหน ยกเว้นข้อ 3 ที่บอกว่าต้องตอบทั้งตำแหน่งและเหตุผล
- ไม่มีแถบคำแนะนำครูค้างบนจอ เพราะมันขึ้นบนแท็บเล็ตนักเรียนด้วย
- ตัวอย่างในด่าน 4 ใช้ **9 → 3** ไม่ใช่ 4 → 2 เพราะ 4 → 2 อ่านได้ทั้ง "รากที่สอง" "หารสอง" และ "ลบสอง"
