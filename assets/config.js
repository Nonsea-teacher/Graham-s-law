/*
 * ค่าคงที่ของสื่อการสอน
 * ครูเป็นผู้ควบคุมทั้งหมด นักเรียนดูตาม จึงไม่มีค่าอะไรให้นักเรียนปรับ
 */

export const LANE = {
  length: 100,        // ความยาวลู่ (หน่วยจำลอง)
  start: 3,           // ตำแหน่งปล่อยตัว
  finish: 90,         // เส้นชัย เว้นที่ด้านหลังไว้ให้เห็นว่าโมเลกุลข้ามไปกองอยู่
  height: 16,         // ความกว้างลู่ในแนวตั้ง
  /*
   * จำนวนโมเลกุลต่อลู่ — ตัวเลขนี้คือความน่าเชื่อถือของบทเรียนโดยตรง
   * ที่ 120 ตัว เวลาที่วัดได้แกว่ง ±11% การแข่งรอบเดียวอาจให้อัตราส่วน 3.2 เท่า
   * แทนที่จะเป็น 2 เท่า ซึ่งทำให้ครูอธิบายต่อไม่ได้
   * ความแกว่งลดลงตาม 1/√N จึงต้องใช้จำนวนมาก เหมือนแก๊สจริงที่มีโมเลกุลมหาศาล
   */
  count: 800,
  T: 760,             // อุณหภูมิจำลอง (kB = 1) เลือกให้ He ใช้เวลาราว 10 วินาที
  dt: 0.02,
};

/** แก๊สที่ใช้ในบทเรียน — ใช้แก๊สจริงทั้งหมด เพื่อให้นักเรียนเชื่อมกับของจริงได้ */
export const GAS = {
  H2:  { key: 'H2',  formula: 'H₂',  name: 'ไฮโดรเจน',     M: 2 },
  He:  { key: 'He',  formula: 'He',  name: 'ฮีเลียม',      M: 4 },
  CH4: { key: 'CH4', formula: 'CH₄', name: 'มีเทน',        M: 16 },
  O2:  { key: 'O2',  formula: 'O₂',  name: 'ออกซิเจน',     M: 32 },
  SO2: { key: 'SO2', formula: 'SO₂', name: 'ซัลเฟอร์ไดออกไซด์', M: 64 },
  NH3: { key: 'NH3', formula: 'NH₃', name: 'แอมโมเนีย',    M: 17 },
  HCl: { key: 'HCl', formula: 'HCl', name: 'ไฮโดรเจนคลอไรด์', M: 36.5 },
  CO2: { key: 'CO2', formula: 'CO₂', name: 'คาร์บอนไดออกไซด์', M: 44 },
};

/*
 * speed = เร่งเวลากี่เท่าเทียบเวลาจริง
 * ตั้งให้การแข่งกระชับ แล้วให้ครูกดปุ่มสโลว์โมชันเมื่ออยากให้ดูละเอียด
 * แทนที่จะบังคับให้ทุกคนดูช้าตลอดทั้งรอบ
 */
export const SLOW_FACTOR = 0.35;

/*
 * ด่าน 2 — แข่งคู่ ไม่มีตัวเลข
 * ครูเลือกคู่แข่งได้ 3 แบบ เพราะ "ชนะขาด" กับ "สูสี" สอนคนละอย่าง
 *   ต่างกันมาก  = เด็กที่อ่อนที่สุดก็เห็น แต่ไม่ตื่นเต้น และต้องรอตัวหนักนาน
 *   สูสี        = สนุกกว่า แต่ต้องมองดี ๆ ถึงจะเห็น
 * อัตราส่วนเวลาที่คาดไว้ = √(M หนัก / M เบา)
 */
export const DUEL_PAIRS = [
  { light: GAS.H2,  heavy: GAS.O2,  label: 'ต่างกันมาก' }, // 4.00 เท่า
  { light: GAS.He,  heavy: GAS.O2,  label: 'ปานกลาง' },   // 2.83 เท่า
  { light: GAS.NH3, heavy: GAS.HCl, label: 'สูสี' },      // 1.47 เท่า — คู่คลาสสิกในห้องแล็บ
];

export const DUEL = { ...DUEL_PAIRS[0], speed: 2.5 };

/*
 * ด่าน 3 — แข่งสามลู่ สามชุด นักเรียนจดผลลงใบงานแล้วหาความสัมพันธ์เอง
 *
 * แต่ละชุดเรียงมวลจากเบาไปหนักเสมอ (สีน้ำเงิน→ส้ม→แดง จึงตรงกับลำดับมวลตลอด)
 * และแต่ละชุดจงใจให้มีคู่ที่อัตราส่วนมวลเป็นเลขกำลังสองพอดี
 * เพื่อให้อัตราส่วนเวลาออกมาเป็นจำนวนเต็มอ่านง่าย
 *   ชุด 1 : 4 → 16 → 64   มวล ×4 ทุกขั้น    เวลา ×2 ทุกขั้น
 *   ชุด 2 : 2 → 32        มวล ×16           เวลา ×4
 *   ชุด 3 : 4 → 36.5      มวล ×9            เวลา ×3
 * รวมสามชุดนักเรียนจะเห็น 4→2, 9→3, 16→4 ซึ่งชี้ไปที่รากที่สองตรง ๆ
 * He โผล่ทั้งชุด 1 และชุด 3 เพื่อให้เห็นว่าวัดซ้ำแล้วได้ค่าใกล้เดิม
 */
export const TRIO_SETS = [
  { label: 'ชุดที่ 1', gases: [GAS.He, GAS.CH4, GAS.SO2] },
  { label: 'ชุดที่ 2', gases: [GAS.H2, GAS.O2, GAS.CO2] },
  { label: 'ชุดที่ 3', gases: [GAS.He, GAS.NH3, GAS.HCl] },
];

export const TRIO = { gases: TRIO_SETS[0].gases, speed: 2.5 };
